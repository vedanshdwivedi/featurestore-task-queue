Transformation:
    __init__(self, dataset_path):
        pass

    load_dataset(self, dataset_path):
        return pd.DataFrame

    load_model(self, model_path):
        return model

    install_requirements():
        return None

    transform(self, dataset_path) -> pd.DataFrame:
        return pd.DataFrame

    train_model(self, model_path):
        return None

    impute(self, dataset_path) -> pd.DataFrame:
        return pd.DataFrame
